// Auth guards are handled client-side in each layout/page component.
// Middleware is not used with static export (output: 'export').
export function middleware() {}
export const config = { matcher: [] };
